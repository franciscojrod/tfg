{#
 * Carousel slide
 *
 * slide_image: Image / Object: Image of the slide.
 * slide_text: String: Body of the slide caption.
 * slide_link_text: String: Text of the button on the slide caption.
 * slide_link_url: String: Link of the button on the slide caption.
#}

<div class="carousel-slide position-relative">
  {% if slide_image %}
    {{ slide_image }}
  {% endif %}
  <div class="carousel-slide__caption col-md-10 offset-md-1 position-absolute">
    {% if slide_text or (slide_link_text and slide_link_url) %}
      <div class="carousel-slide__caption-inner d-inline-block px-2 py-4 mb-5">
        {% if slide_text %}
          <h2 class="carousel-slide__title">{{ slide_text }}</h2>
        {% endif %}
        {% if slide_link_text and slide_link_url %}
          {% include '@atoms/button/_button.twig' with {
              button_color: 'primary',
              button_element: 'a',
              button_text: slide_link_text,
              button_link: slide_link_url,
              button_icon_name: 'chevron_right',
            } %}
        {% endif %}
      </div>
    {% endif %}
  </div>
</div>
