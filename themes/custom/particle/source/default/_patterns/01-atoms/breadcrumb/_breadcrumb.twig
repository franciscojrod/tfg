{#
/**
 * @file Breadcumb!
 * Documenting Breadcrumb Examples from Bootstrap.
 *
 * Available variables:
 * - breadcrumb_items: [array] A list of breadcrumbs to render
 * - breadcrumb_title: [string] The title of the breadcrumb
 * - breadcrumb_link: [string] The URL of the breadcrumb
 *
 * @see https://www.w3.org/TR/wai-aria-practices/#breadcrumb
 * @see https://getbootstrap.com/docs/4.0/components/breadcrumb/
 */
#}

<nav aria-label="breadcrumb" role="navigation">
  <ol class="breadcrumb">
    {% for item in breadcrumb %}
      {% if loop.last %}
        <li class="breadcrumb__item active is-active" aria-current="page">
          {% if not loop.first %}
            {% include '@atoms/icon/_icon.twig' with { icon_name: 'chevron_right' } %}
          {% endif %}
          {{ item.text }}
        </li>

      {% else %}
        <li class="breadcrumb__item">
          {% if not loop.first %}
            {% include '@atoms/icon/_icon.twig' with { icon_name: 'chevron_right' } %}
          {% endif %}
          <a class="breadcrumb__link" href="{{ (pl) ? '#' : item.url }}">{{ item.text }}</a>
        </li>
      {% endif %}
    {% endfor %}
  </ol>
</nav>
