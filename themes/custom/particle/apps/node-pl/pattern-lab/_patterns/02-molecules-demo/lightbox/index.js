/**
 * Demo of lightbox. Pulls in lightbox assets, and provides demo-only assets.
 *
 * (This file is NOT imported by the design system, but is included as part of
 * a Pattern Lab app.)
 */

// Import component assets
import 'molecules/lightbox';

// Import demo assets
import twig from './lightboxs.twig';
import yaml from './lightboxs.yml';

export default {
  twig,
  yaml,
};
